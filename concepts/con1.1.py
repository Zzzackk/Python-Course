# Concept: Using the function len to return the numbers of letters(items) in the Title given by the user.

user_prompt = "Enter title:"

title = input(user_prompt)

length = len(title)

print("The length of the title is:",length)